#include <stdio.h>
/**
 * main - prints name of program
 * @argv: arg vector
 * @argc: number of arg
 *
 * Return: (0)
 */
int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
