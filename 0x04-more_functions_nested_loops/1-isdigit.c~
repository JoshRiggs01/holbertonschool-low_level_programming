#include "main.h"
/**
 * _isdigit - checks for digit
 * main - entry point
 *@c: digit
 * Return: (1) for success (0) for fail
 */
int _isdigit(int c)
{
	if (c >= 0 && c <= 9)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
