#include "main.h"
/**
 * _islower - checks for lowercase
 * main - check the code
 * @c: lowercase
 * Return - Always 0
 */
int _islower(int c)
{
if (c >= 'a' && c <= 'z')
{
return (1);
}
else
{
return (0);
}
}
