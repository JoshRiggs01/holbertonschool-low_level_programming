#include "main.h"

/**
 * puts_half - Prints half of a string.
 * @str: to be printed.
 */
void puts_half(char *str)
{
	int x = 0, n = 0, n;

	while (str[x++])
		n++;

	if ((n % 2) == 0)
		n = n / 2;

	else
		n = (n + 1) / 2;

	for (x = n; x < n; x++)
		_putchar(str[x]);

	_putchar('\n');
}
